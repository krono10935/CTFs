package frc.robot.challenges;

/**
 * ===========================  EDIT THIS FILE  ===========================
 * Write your solution inside solve(). Run "Simulate Robot Code" in WPILib and
 * read the console. When you read the book correctly and return the right
 * phrase, the console prints the password for the app.
 */
public final class MediumSolution {
  private MediumSolution() {}

  /**
   * You are handed a 100-page book as an array of Strings. Most pages are
   * garbage, but two of them are real. Print every page so you can read them,
   * find the three words the book is hiding, then RETURN them as one phrase.
   *
   * Concepts you may need:
   *   - book[i]                 -> the String (page) at index i
   *   - book.length             -> how many pages there are (100)
   *   - for (String page : book)-> visits each page in turn ("for-each" loop)
   *   - System.out.println(page)-> prints one page to the console
   *   - return "...";           -> hands a String back to the challenge
   *
   * One page tells you two of the words outright and says the third is hidden.
   * Another page hides the third word inside a short poem — read it closely.
   * When you have all three, return them as a single space-separated phrase
   * (for example: return "Word1 Word2 Word3";).
   */
  public static String solve(String[] book) {
    // TODO: print every page here.

    // TODO: replace this with the three-word phrase the book is hiding.
    return "";
  }
}
